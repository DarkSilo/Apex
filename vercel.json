{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "if [ -f package.json ]; then npm install; else npm install --prefix client; fi",
  "buildCommand": "if [ -f package.json ]; then npm run build; else npm run build --prefix client; fi",
  "devCommand": "if [ -f package.json ]; then npm run dev; else npm run dev --prefix client; fi"
}
